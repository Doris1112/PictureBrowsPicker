<?xml version="1.0" encoding="utf-8"?>
<com.doris.picture.library.picker.widget.SquareFrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?itemPlaceholder"
    android:foreground="?selectableItemBackground">

    <TextView
        android:id="@+id/picture_picker_hint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:drawableTop="@drawable/picture_picker_camera"
        android:drawablePadding="8dp"
        android:gravity="center"
        android:text="@string/photo_grid_capture"
        android:textColor="?captureTextColor"
        android:textSize="14sp" />

</com.doris.picture.library.picker.widget.SquareFrameLayout>